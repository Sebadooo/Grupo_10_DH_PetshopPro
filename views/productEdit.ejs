<!DOCTYPE html>
<html>
<%- include('partials/head.ejs') %>

    <body>

        <%- include('partials/header.ejs') %>

            <%- include('partials/navbar.ejs') %>

                <main>
                    <h3 class="título">EDITAR USUARIO</h3>
                    <div class="errorsFront">
                       <ul>

                       </ul> 
                    </div>
                    <section class="formularios">
                        <div class="bannersLat">
                            <img class="bannerLat" src="/images/otros/Banner_Perro_Izq.jpg" />
                          </div>
                        <form class="edicion" action="userEdit/<%userToEdit.id%>" method="POST" enctype="multipart/form-data">

                            <!-- < % if (locals.errors) { %>
                                < % for (let oneError in errors) { %>
                                    <p>
                                        < %= errors[oneError].msg %>
                                    </p>
                                    < % } %>
                                        < % } %> -->    
                            <p class="etiquetas1">  
                                <label for="nombre">Nombre: </label>
                            </p>
                            <p class="input">
                                <input type="text" name="first_name" value="<%=userToEdit.first_name%>" class="first_name">
                            </p>
                        
                            <!-- < % if(locals.errors && errors.first_name) { %>
                                <p class="errorEpress">
                                    < %= errors.first_name.msg %>
                                </p>
                                < % } %> -->
                            <!-- < % if (locals.errors && errors.first_name) { %>
                                                <div class="text-danger">
                                                    < %= errors.first_name.msg %>
                                                    < % } %> -->
                                                    
                                                </div>
                            <p class="etiquetas1">
                                <label for="apellido">Apellido: </label>
                            </p>
                            <p class="input">
                                <input type="text" name="last_name" value="<%=userToEdit.last_name%>" class="last_name">
                                
                            </p>
                            <!-- < % if (locals.errors && errors.last_name) { %>
                                                        <div class="text-danger">
                                                            < %= errors.last_name.msg %>
                                                            < % } %> -->
                            <p class="etiquetas1">
                                <label for="email">E-mail: </label>
                            </p>
                            <p class="input">
                                <input type="email" name="email" value="<%=userToEdit.email%>" class="email">
                                
                            </p>
                            <!-- < % if (locals.errors && errors.email) { %>
                                                        <div class="text-danger">
                                                            < %= errors.email.msg %>
                                                            < % } %> -->
                            <p class="etiquetas1">
                                <label for="clave">Nueva Contraseña: </label>
                            </p>
                            <p class="input">
                                <input type="password" name="password" class="password">
                                
                            </p>
                            <!-- < % if (locals.errors && errors.password %>
                                                        <div class="text-danger">
                                                            < %= errors.passwordmsg %>
                                                            < % } %> -->
                            <p class="etiquetas1">
                                <label for=""> Categoria: </label>  
                                <select class="input" name="category">
                                    <% for (let i = 0 ; i < category.length ; i++) { %>
                                        <% if (category[i].product_id == productToEdit.product_category_id){%>

                                            <option value="<%=category[i].product_category_id%>" selected>
                                                <%=category.category_name%>
                                            </option>
                                        <% } else { %>
                                            <option value="<%=category[i].product_category_id%>">

                                            <%=category.category_name%>
                                            </option>
                                        <% } %>
                                    <% } %>
                                </p>
                            
                            <p class="etiquetas1">
                                <label for="avatar">Foto de perfil: </label>
                            </p>
                            <p class="botonFile">    
                                <input type="file" id="avatar" name="avatar" value="<%=userToEdit.avatar%>" accept=".jpg,.jpeg,.png,.gif">
                            </p>
                            
                            <p class="boton1">
                                <button type="submit">GUARDAR CAMBIOS</button>
                            </p>
                        </form>
                        <div class="bannersLat">
                            <img class="bannerLat" src="/images/otros/Banner_Gato_Der.jpg" />
                          </div>
                    </section>
                    <!-- <script src="/javascript/registerVal.js" type="text/javascript"></script> -->
                </main>

                <%- include('partials/footer.ejs') %>

    </body>

</html>