<!DOCTYPE html>
<html>
<%- include('partials/head.ejs') %>

    <body>

        <%- include('partials/header.ejs') %>

            <%- include('partials/navbar.ejs') %>

                <main>
                    <h3 class="título">CREAR USUARIO</h3>
                    <div class="errorsFront">
                       <ul>

                       </ul> 
                    </div>
                    <section class="formularios">
                    
                        <div class="bannersLat">
                            <img src="/images/otros/Banner_Perro_Izq.jpg" />
                          </div>
                        <form class="registro" action="/userCreate" method="POST" enctype="multipart/form-data">

                            <!-- < % if (locals.errors) { %>
                                < % for (let oneError in errors) { %>
                                    <p>
                                        < %= errors[oneError].msg %>
                                    </p>
                                    < % } %>
                                        < % } %> -->    
                            <p class="etiquetas1">  
                                <label for="nombre">Nombre: </label>
                            </p>
                            <p class="input">
                                <input type="text" name="first_name" class="first_name" required>
                                <i class="fas fa-check-circle"></i>
                            </p>
                        
                            <!-- < % if(locals.errors && errors.first_name) { %>
                                <p class="errorEpress">
                                    < %= errors.first_name.msg %>
                                </p>
                                < % } %> -->
                            <!-- < % if (locals.errors && errors.first_name) { %>
                                                <div class="text-danger">
                                                    < %= errors.first_name.msg %>
                                                    < % } %> -->
                                                    
                                                
                            <p class="etiquetas1">
                                <label for="apellido">Apellido: </label>
                            </p>
                            <p class="input">
                                <input type="text" name="last_name" class="last_name" required>
                                <i class="fas fa-check-circle"></i>
                                <i class="fas fa-exclamation-circle"></i>
                                <small>error mesage</small>
                            </p>
                            <!-- < % if (locals.errors && errors.last_name) { %>
                                                        <div class="text-danger">
                                                            < %= errors.last_name.msg %>
                                                            < % } %> -->
                            <p class="etiquetas1">
                                <label for="email">E-mail: </label>
                            </p>
                            <p class="input">
                                <input type="email" name="email" class="email" required>
                                                            </p>
                            <!-- < % if (locals.errors && errors.email) { %>
                                                        <div class="text-danger">
                                                            < %= errors.email.msg %>
                                                            < % } %> -->
                            <p class="etiquetas1">
                                <label for="clave">Contraseña: </label>
                            </p>
                            <p class="input">
                                <input type="password" name="password" class="password" required>
                                <i class="fas fa-check-circle"></i>
                                <i class="fas fa-exclamation-circle"></i>
                                <small>error mesage</small>
                            </p>
                            <!-- < % if (locals.errors && errors.password %>
                                                        <div class="text-danger">
                                                            < %= errors.passwordmsg %>
                                                            < % } %> -->
                            <!-- <p class="etiquetas1">
                            <label for="clave">Confirma la contraseña: </label>
                        </p> -->
                           <p class="etiquetas1">
                            <label for=""> Categoria: </label>  
                            <select name="category">
                                <% for (let i = 0 ; i < userCat.length ; i++) { %>
                                <option class="input" value="<%=userCat[i].id%>">
                                    <%=userCat[i].description%>
                                </option>
                                <% } %>
                            </p>
                            <br>
                            <p class="etiquetas1">
                                <label for="avatar">Avatar: </label>
                            </p>
                            <p class="botonFile">    
                                <input type="file" id="avatar" name="avatar" accept=".jpg,.jpeg,.png,.gif" multiple>
                            </p>
                            <!-- < % if (locals.errors && errors.avatar) { %>
                                                        <div class="text-danger">
                                                            < %= errors.avatar.msg %>
                                                            < % } %> -->
                            <p class="boton1">
                                <button type="submit">CREAR</button>
                            </p>
                        </form>
                    </div>
                        <div class="bannersLat">
                            <img src="images/otros/Banner_Gato_Der.jpg" />
                          </div>
                    </section>
                    <script src="/javascript/registerVal.js" type="text/javascript"></script>
                </main>

                <%- include('partials/footer.ejs') %>

    </body>

</html>